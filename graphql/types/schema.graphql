# import Complete from 'complete.graphql'
# import Search from 'search.graphql'
# import CompleteInput from 'complete-input.graphql'
# import SearchInput from 'search-input.graphql'

type Query {
	complete(completion_input: CompleteInput): Complete
	search(search_input: SearchInput): [Search]
}